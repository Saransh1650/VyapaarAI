name: ai_khata
description: "Retail Analytics AI System — MVP"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  # HTTP
  dio: ^5.4.3
  # Navigation
  go_router: ^14.2.7
  # State management
  provider: ^6.1.2
  # Local storage
  shared_preferences: ^2.3.2
  # Environment
  flutter_dotenv: ^5.1.0
  # Charts
  fl_chart: ^0.69.0
  # Image picking / camera
  image_picker: ^1.1.2
  # Date formatting
  intl: ^0.19.0
  # Icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
